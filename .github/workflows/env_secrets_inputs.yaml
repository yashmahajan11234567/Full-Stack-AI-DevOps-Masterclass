name: Demo CI Env Secrets

on:
    # push:
    workflow_dispatch   

jobs:
    build:
        runs-on: ubuntu-latest

        steps:

            - name: Say Hello
              run: echo Hello, world!

            - name: install
              env:
                APP_ENV: demo
              run: echo "Environment $APP_ENV"
