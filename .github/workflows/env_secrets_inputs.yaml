name: Demo CI Env Secrets

on:
    push:
    # workflow_dispatch   

jobs:
    build:
        runs-on: ubuntu-latest

        steps:

            - name: Say Hello
              run: echo Hello, world!

            - name: install
              env:
                APP_ENV: dev
                DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
              run: echo "Environment $APP_ENV using $DB_PASSWORD"
